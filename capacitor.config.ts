const config = {
  appId: 'com.vaultos.app',
  appName: 'VaultOS',
  webDir: 'dist',
  plugins: {
    SplashScreen: {
      launchShowDuration: 2500,
      launchAutoHide: true,
      backgroundColor: "#000000",
      showSpinner: false
    }
  }
}

export default config
